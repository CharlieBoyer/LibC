/*
** EPITECH PROJECT, 2018
** my_strncat
** File description:
** Concatenate the first "n" char of two strings
*/

/*
#include "lib.h"

char *my_strncat(char *dest, char const *src, int nb)
{
    int a = my_strlen(src);
    int b = my_strlen(dest);
    int i = a + b;
    char result[i];
    int c = 0;
    int d = 0;

    while (c < a) {
        result[c] = src[c];
        ++c;
    }

    while (nb != 0) {
        result[c] = dest[d];
        --nb;
        ++d;
        ++c;
    }
    return (result);
}
*/